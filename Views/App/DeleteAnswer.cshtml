@model ApplicationForTest.Models.Answer


@{
    ViewData["Title"] = "Delete Question";
}

<h1>Delete Answer</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Answer</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.isRight)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.isRight)
        </dd>
        

    </dl>

    <form asp-action="DeleteAnswer" asp-route-id="@Model.QuestionId">
        <input type="hidden" asp-for="Id" />
        <div class="form-group">
            <button type="submit" value="Delete" class="btn btn-danger">
                Delete
            </button>


            <a asp-action="IndexTests" asp-route-id="@Model.QuestionId">Back to List</a>
        </div>
        
            
    </form>
</div>
